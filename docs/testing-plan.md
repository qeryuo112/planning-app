# 计划型 App 测试计划（Week 27 个人使用版）

## 1. 计划概述

### 1.1 测试目标

验证 Week 27 个人使用版本在 Android 真机与 Windows 桌面端的可用性、稳定性与核心功能完整性，确保正式上线后满足个人日常使用需求。

主要关注点：
- 核心功能闭环：注册/登录 → 目标/任务/习惯 → AI 计划 → 执行 → 复盘
- 数据一致性：本地 SQLite、服务端 PostgreSQL、多端同步的一致性
- 离线可用性：弱网/断网场景下操作不丢失，联网后自动同步
- 稳定性：无崩溃、无白屏、无长时间无响应
- 性能：启动时间、页面加载、AI 响应时间可接受

### 1.2 测试范围

**在测范围**
- Flutter 客户端：Android APK、Windows exe
- 后端 API：部署在 `https://xutaostudy.xyz/api/v1` 的 NestJS 服务
- 本地数据库：SQLite 离线存储与同步队列
- 核心功能：登录、今日页、目标、任务、习惯、AI 计划、复盘、收件箱、日历、提醒、设置、报表、运动数据导入

**不在测范围**
- 商业化功能（社交、小组挑战、排行榜深度测试）
- 应用商店审核、支付、订阅系统
- iOS/macOS 客户端（当前未构建）
- 第三方 OAuth 深度集成（Google 日历 OAuth 仅验证基础流程）

### 1.3 测试周期

建议 3 天：

| 天数 | 内容 |
|------|------|
| Day 1 | 环境准备、安装、冒烟测试、登录/今日/目标/任务基础流程 |
| Day 2 | 习惯、AI 计划、复盘、收件箱、日历、提醒、设置、报表 |
| Day 3 | 离线同步、弱网、性能、兼容性、Windows 桌面端、回归测试、报告 |

### 1.4 测试执行者

- 主测：个人日常使用
- 协助：可邀请 1 台 Android 真机 + 1 台 Windows 电脑辅助验证

---

## 2. 测试环境

### 2.1 服务端

- 生产环境：`https://xutaostudy.xyz/api/v1`
- 健康检查：`GET https://xutaostudy.xyz/api/v1/health`
- 部署版本：Week 27 个人使用版

### 2.2 客户端

| 平台 | 最低要求 | 建议测试设备 |
|------|----------|--------------|
| Android | Android 9+（API 28+），实际 minSdk 26 | 1 台 Android 12/13/14 真机 + 1 台 Android 11 模拟器 |
| Windows | Windows 10 64 位 | 1 台 Windows 10/11 电脑 |

### 2.3 网络环境

- Wi-Fi（正常网络）
- 移动数据（4G/5G）
- 弱网（可通过 Android 开发者选项或 Charles/Fiddler 限速）
- 离线（关闭网络后操作）

### 2.4 测试账号

| 字段 | 值 |
|------|-----|
| 邮箱 | `planning-test@example.com` |
| 密码 | `Test@123456` |

> 注意：公开测试账号，请勿录入真实个人信息。测试完成后建议修改密码或创建新账号。

---

## 3. 测试策略

### 3.1 功能测试

按模块逐一执行正向流程与异常流程，覆盖 CRUD、状态变化、边界条件。

### 3.2 探索性测试

无固定脚本，模拟真实个人使用场景：早晨打开 App 查看今日任务、创建今日目标、睡前复盘、添加临时灵感到收件箱。

### 3.3 离线/同步测试

- 断网后执行操作，检查本地数据库是否保存
- 恢复网络后检查是否自动同步到服务器
- 多端操作同一数据，检查冲突处理

### 3.4 性能测试

- 启动时间：从点击图标到今日页可交互
- 页面加载：进入各主页面耗时
- AI 响应：从请求到返回结果耗时
- 内存占用：长时间运行后内存变化

### 3.5 兼容性测试

- Android 不同屏幕尺寸（手机、折叠屏）
- Windows 不同分辨率与缩放比例（100%、125%、150%）
- 深色模式（如已实现）

### 3.6 后端 API 测试

- 通过 Swagger `/docs` 或 curl 验证核心接口
- 验证错误码、鉴权、参数校验

---

## 4. 测试用例

### 用例格式说明

每条用例包含：
- **ID**：唯一编号
- **模块**：所属功能模块
- **优先级**：P0（阻塞）、P1（重要）、P2（一般）
- **场景**：测试点描述
- **前置条件**：执行前需要满足的条件
- **步骤**：操作序列
- **预期结果**：成功标准
- **实测结果**：测试时填写（通过/失败/阻塞）
- **备注**：补充说明、日志关键词、截图位置

---

### 4.1 注册/登录模块

#### TC-AUTH-001 [P0] 注册新账号
- **步骤**：
  1. 打开 App，进入注册页
  2. 输入未注册邮箱、8 位以上密码
  3. 点击注册
- **预期结果**：注册成功，自动进入今日页，本地保存 token
- **实测结果**：
- **备注**：

#### TC-AUTH-002 [P0] 使用测试账号登录
- **步骤**：
  1. 打开 App，进入登录页
  2. 输入 `planning-test@example.com` / `Test@123456`
  3. 点击登录
- **预期结果**：登录成功，进入今日页，头像/昵称显示正常
- **实测结果**：
- **备注**：

#### TC-AUTH-003 [P1] 登录密码错误
- **步骤**：
  1. 输入正确邮箱、错误密码
  2. 点击登录
- **预期结果**：提示“邮箱或密码错误”，不进入首页
- **实测结果**：
- **备注**：

#### TC-AUTH-004 [P1] Token 过期自动刷新
- **前置条件**：已登录并等待较长时间（或修改本地时间模拟过期）
- **步骤**：
  1. 重新打开 App
  2. 执行一次需要鉴权的操作（如拉取今日数据）
- **预期结果**：自动使用 refreshToken 获取新 accessToken，操作成功
- **实测结果**：
- **备注**：

#### TC-AUTH-005 [P1] 退出登录
- **步骤**：
  1. 进入设置页
  2. 点击退出登录
- **预期结果**：清除本地 token，返回登录页
- **实测结果**：
- **备注**：

---

### 4.2 今日页模块

#### TC-TODAY-001 [P0] 今日页加载
- **步骤**：
  1. 登录后进入今日页
- **预期结果**：显示 Top 3 任务、习惯打卡列表、目标进度、过期任务（如有）
- **实测结果**：
- **备注**：

#### TC-TODAY-002 [P0] 完成今日任务
- **步骤**：
  1. 在今日页点击任务完成按钮
- **预期结果**：任务状态变为完成，目标进度更新，本地与服务端一致
- **实测结果**：
- **备注**：

#### TC-TODAY-003 [P0] 习惯打卡
- **步骤**：
  1. 在今日页点击习惯打卡
- **预期结果**：打卡成功，连续天数 +1，状态同步
- **实测结果**：
- **备注**：

#### TC-TODAY-004 [P1] 过期任务展示
- **前置条件**：存在未完成的过期任务
- **步骤**：
  1. 进入今日页
- **预期结果**：过期任务单独显示，可点击完成或延期
- **实测结果**：
- **备注**：

#### TC-TODAY-005 [P1] 明日预览
- **步骤**：
  1. 在今日页点击「明日预览」入口
- **预期结果**：展示明日任务/计划列表
- **实测结果**：
- **备注**：

---

### 4.3 目标模块

#### TC-GOAL-001 [P0] 创建目标
- **步骤**：
  1. 进入目标页
  2. 点击新建
  3. 输入标题、描述、时间范围、类型
  4. 保存
- **预期结果**：目标出现在列表，服务端返回新目标
- **实测结果**：
- **备注**：

#### TC-GOAL-002 [P0] 查看目标进度
- **步骤**：
  1. 进入目标页
  2. 点击某个目标
- **预期结果**：显示进度条、连续天数、里程碑时间线
- **实测结果**：
- **备注**：

#### TC-GOAL-003 [P1] 编辑目标
- **步骤**：
  1. 在目标详情页点击编辑
  2. 修改标题/截止时间
  3. 保存
- **预期结果**：修改生效，列表同步更新
- **实测结果**：
- **备注**：

#### TC-GOAL-004 [P1] 删除目标
- **步骤**：
  1. 在目标详情页删除目标
- **预期结果**：目标从列表移除，关联任务/习惯处理符合预期（提示或解除关联）
- **实测结果**：
- **备注**：

#### TC-GOAL-005 [P1] 目标分享（个人版轻测）
- **步骤**：
  1. 在目标列表点击分享按钮
- **预期结果**：弹出分享选项或复制链接，不崩溃
- **实测结果**：
- **备注**：个人版不深度验证社交功能

---

### 4.4 任务模块

#### TC-TASK-001 [P0] 创建任务
- **步骤**：
  1. 进入任务页
  2. 点击新建
  3. 输入标题、日期、能量等级、权重、关联目标
  4. 保存
- **预期结果**：任务出现在列表和今日页
- **实测结果**：
- **备注**：

#### TC-TASK-002 [P0] 完成任务
- **步骤**：
  1. 在任务页点击完成
- **预期结果**：任务状态更新，目标进度增加
- **实测结果**：
- **备注**：

#### TC-TASK-003 [P1] 任务筛选排序
- **步骤**：
  1. 在任务页切换筛选条件：状态/能量/日期/排序
- **预期结果**：列表按条件正确过滤排序
- **实测结果**：
- **备注**：

#### TC-TASK-004 [P1] 任务延期
- **步骤**：
  1. 选择一个过期任务
  2. 点击延期，选择新日期
- **预期结果**：任务日期更新，从过期列表移除
- **实测结果**：
- **备注**：

#### TC-TASK-005 [P2] 任务删除
- **步骤**：
  1. 删除一条任务
- **预期结果**：任务从列表和服务端移除
- **实测结果**：
- **备注**：

---

### 4.5 习惯模块

#### TC-HABIT-001 [P0] 创建习惯
- **步骤**：
  1. 进入习惯页
  2. 新建习惯，设置频率、能量等级、关联目标
  3. 保存
- **预期结果**：习惯出现在列表和今日页
- **实测结果**：
- **备注**：

#### TC-HABIT-002 [P0] 习惯打卡
- **步骤**：
  1. 在习惯页或今日页点击打卡
- **预期结果**：打卡成功，连续天数更新
- **实测结果**：
- **备注**：

#### TC-HABIT-003 [P1] 习惯筛选排序
- **步骤**：
  1. 在习惯页切换筛选：频率/能量/排序
- **预期结果**：列表正确过滤排序
- **实测结果**：
- **备注**：

#### TC-HABIT-004 [P1] 连续打卡天数
- **前置条件**：连续多日打卡同一习惯
- **步骤**：
  1. 查看习惯详情
- **预期结果**：最长连续天数与当前连续天数正确
- **实测结果**：
- **备注**：

---

### 4.6 AI 计划模块

#### TC-AI-001 [P0] 创建 AI 计划草案
- **步骤**：
  1. 进入 AI 计划页
  2. 输入目标描述（如“考研英语”）
  3. 选择模板或自定义 planDuration/stageLength
  4. 点击生成
- **预期结果**：返回分阶段计划，任务落库或进入确认页
- **实测结果**：
- **备注**：

#### TC-AI-002 [P0] AI 计划分阶段展示
- **步骤**：
  1. 生成计划后查看阶段概览
- **预期结果**：显示当前阶段、总阶段数、每阶段任务
- **实测结果**：
- **备注**：

#### TC-AI-003 [P1] 进入下一阶段
- **步骤**：
  1. 在当前阶段完成后点击「进入下一阶段」
- **预期结果**：下一阶段任务激活，上一阶段任务不再重复显示
- **实测结果**：
- **备注**：接口 `POST /ai/plan-drafts/:id/advance`

#### TC-AI-004 [P1] 流式生成
- **步骤**：
  1. 使用 SSE 流式生成计划
- **预期结果**：内容逐字显示，不卡顿，最终可确认落库
- **实测结果**：
- **备注**：

#### TC-AI-005 [P1] AI 用量卡片
- **步骤**：
  1. 在 AI 计划页查看用量
- **预期结果**：显示当日/当月用量，接近上限时提示降级
- **实测结果**：
- **备注**：

#### TC-AI-006 [P1] 继续对话
- **步骤**：
  1. 在 AI 计划页点击「继续对话」
  2. 输入 follow-up 问题
- **预期结果**：AI 基于上下文回复，消息列表更新
- **实测结果**：
- **备注**：

#### TC-AI-007 [P2] AI 日费用上限降级
- **前置条件**：当日 AI 调用费用达到上限
- **步骤**：
  1. 再次请求 AI 生成计划
- **预期结果**：优先使用模板草案，不触发真实模型调用
- **实测结果**：
- **备注**：

#### TC-AI-008 [P2] 超载警告
- **步骤**：
  1. 创建大量任务导致计划负载过高
- **预期结果**：提示超载警告，建议减少任务或延长期限
- **实测结果**：
- **备注**：

---

### 4.7 AI 复盘/洞察模块

#### TC-REVIEW-001 [P1] 生成 AI 复盘
- **步骤**：
  1. 进入复盘页
  2. 选择时间范围
  3. 点击生成 AI 复盘
- **预期结果**：返回总结、洞察、下一步建议
- **实测结果**：
- **备注**：

#### TC-REVIEW-002 [P1] AI 洞察页刷新
- **步骤**：
  1. 进入 AI 洞察页
  2. 查看画像最后刷新时间
  3. 手动刷新
- **预期结果**：刷新成功，时间更新，推荐内容更新
- **实测结果**：
- **备注**：

#### TC-REVIEW-003 [P2] 重新规划
- **步骤**：
  1. 在复盘页选择重新规划
- **预期结果**：基于当前进度生成新的计划草案
- **实测结果**：
- **备注**：

---

### 4.8 收件箱模块

#### TC-INBOX-001 [P0] 添加收件箱条目
- **步骤**：
  1. 进入更多页 → 收件箱
  2. 输入灵感/临时想法
  3. 保存
- **预期结果**：条目出现在列表，本地优先保存，服务端同步
- **实测结果**：
- **备注**：

#### TC-INBOX-002 [P0] 整理到目标/任务
- **步骤**：
  1. 在收件箱条目点击整理
  2. 选择转换为任务或关联目标
- **预期结果**：生成对应任务/目标，收件箱条目标记为已整理
- **实测结果**：
- **备注**：接口 `POST /inbox/:id/convert`

#### TC-INBOX-003 [P1] 忽略收件箱条目
- **步骤**：
  1. 在收件箱条目点击忽略
- **预期结果**：条目从列表移除或标记为忽略
- **实测结果**：
- **备注**：接口 `POST /inbox/:id/dismiss`

#### TC-INBOX-004 [P1] 离线添加收件箱
- **步骤**：
  1. 关闭网络
  2. 添加收件箱条目
  3. 恢复网络
- **预期结果**：离线条目保存到本地，联网后自动同步到服务端
- **实测结果**：
- **备注**：

---

### 4.9 日历模块

#### TC-CAL-001 [P1] 创建日历事件
- **步骤**：
  1. 进入更多页 → 日历
  2. 点击新建事件
  3. 输入标题、时间、提醒
  4. 保存
- **预期结果**：事件显示在月视图和列表中
- **实测结果**：
- **备注**：

#### TC-CAL-002 [P1] 编辑/删除日历事件
- **步骤**：
  1. 在日历页点击已有事件
  2. 修改时间或删除
- **预期结果**：修改生效，界面同步
- **实测结果**：
- **备注**：

#### TC-CAL-003 [P1] ICS 导入
- **步骤**：
  1. 选择一个 ICS 文件导入
- **预期结果**：事件批量导入成功，显示在日历中
- **实测结果**：
- **备注**：

#### TC-CAL-004 [P2] ICS 导出
- **步骤**：
  1. 导出日历事件为 ICS 文件
- **预期结果**：生成文件，内容格式正确
- **实测结果**：
- **备注**：

#### TC-CAL-005 [P2] 外部日历订阅
- **步骤**：
  1. 添加一个 ICS / Google / Outlook 订阅 URL
  2. 等待轮询或手动刷新
- **预期结果**：订阅事件显示在日历中
- **实测结果**：
- **备注**：

#### TC-CAL-006 [P1] 日历离线操作
- **步骤**：
  1. 关闭网络
  2. 创建/编辑/删除事件
  3. 恢复网络
- **预期结果**：本地操作生效，联网后同步到服务端
- **实测结果**：
- **备注**：

---

### 4.10 提醒/通知模块

#### TC-REM-001 [P1] 创建提醒
- **步骤**：
  1. 创建一条带提醒的任务或事件
  2. 设置到期时间
- **预期结果**：提醒记录保存，到期后触发通知
- **实测结果**：
- **备注**：

#### TC-REM-002 [P1] 本地通知触发（Android）
- **步骤**：
  1. 设置 1 分钟后到期的提醒
  2. 等待或调整系统时间
- **预期结果**：系统通知栏出现提醒，点击后跳转今日页
- **实测结果**：
- **备注**：需授予精确闹钟权限

#### TC-REM-003 [P1] 通知权限引导
- **步骤**：
  1. 在未授权通知的新设备上首次打开 App
- **预期结果**：弹出权限引导，授权后通知功能正常
- **实测结果**：
- **备注**：

#### TC-REM-004 [P2] 提醒忽略/延后
- **步骤**：
  1. 通知触发后点击忽略或延后
- **预期结果**：提醒状态更新，延后提醒重新定时
- **实测结果**：
- **备注**：接口 `POST /reminders/:id/dismiss` / `snooze`

#### TC-FCM-001 [P2] FCM Token 上传
- **步骤**：
  1. 在 Android 真机登录
  2. 查看后端是否收到 fcmToken
- **预期结果**：token 上传成功，后台可收到远程推送
- **实测结果**：
- **备注**：

> 注意：Windows 桌面版当前不支持本地通知和 FCM 远程推送，无需测试。

---

### 4.11 设置/偏好模块

#### TC-SET-001 [P1] 修改时区
- **步骤**：
  1. 进入设置页
  2. 修改时区
  3. 保存
- **预期结果**：时区保存成功，今日页日期/时间显示正确
- **实测结果**：
- **备注**：

#### TC-SET-002 [P1] 修改可用时间
- **步骤**：
  1. 在设置页设置每日可用时间块
  2. 保存
- **预期结果**：可用时间保存，AI 计划生成时考虑该时间
- **实测结果**：
- **备注**：

#### TC-SET-003 [P1] 修改精力曲线
- **步骤**：
  1. 在设置页设置不同时段精力等级
  2. 保存
- **预期结果**：精力曲线保存，AI 计划任务时间匹配精力曲线
- **实测结果**：
- **备注**：

#### TC-SET-004 [P1] 通知偏好设置
- **步骤**：
  1. 在设置页开启/关闭各类通知开关
- **预期结果**：开关状态保存，对应通知行为生效
- **实测结果**：
- **备注**：

#### TC-SET-005 [P1] 修改密码
- **步骤**：
  1. 在设置页修改密码
  2. 使用新密码重新登录
- **预期结果**：新密码生效，旧密码失效
- **实测结果**：
- **备注**：

---

### 4.12 报表模块

#### TC-REP-001 [P1] 执行报表
- **步骤**：
  1. 进入更多页 → 报表
  2. 切换到执行 Tab
- **预期结果**：展示任务/习惯完成率、趋势图
- **实测结果**：
- **备注**：

#### TC-REP-002 [P1] 能量报表
- **步骤**：
  1. 切换到能量 Tab
- **预期结果**：展示不同时段能量使用分布
- **实测结果**：
- **备注**：

#### TC-REP-003 [P1] 最佳时段报表
- **步骤**：
  1. 切换到最佳时段 Tab
- **预期结果**：展示高完成率时段推荐
- **实测结果**：
- **备注**：

#### TC-REP-004 [P2] 报表缓存
- **步骤**：
  1. 多次进入报表页
- **预期结果**：非首次加载速度更快，数据一致
- **实测结果**：
- **备注**：后端 Redis 缓存 TTL 1 小时

---

### 4.13 运动数据模块

#### TC-FIT-001 [P2] 单条运动数据导入
- **步骤**：
  1. 进入运动数据导入页
  2. 手动输入一条运动记录
  3. 关联到习惯
- **预期结果**：记录保存，习惯自动生成打卡
- **实测结果**：
- **备注**：

#### TC-FIT-002 [P2] JSON 批量导入
- **步骤**：
  1. 上传 JSON 文件批量导入运动数据
- **预期结果**：批量导入成功，错误记录有提示
- **实测结果**：
- **备注**：

#### TC-FIT-003 [P2] Health Connect 同步（Android）
- **前置条件**：设备已安装 Health Connect，授权读取权限
- **步骤**：
  1. 点击 Health Connect 同步
- **预期结果**：读取运动数据并关联习惯，失败时显示警告卡片并提供 JSON 导入回退
- **实测结果**：
- **备注**：

---

### 4.14 离线/同步模块

#### TC-SYNC-001 [P0] 离线创建任务后同步
- **步骤**：
  1. 关闭网络
  2. 创建任务
  3. 打开网络
  4. 等待或下拉刷新
- **预期结果**：任务同步到服务端，其他端可见
- **实测结果**：
- **备注**：

#### TC-SYNC-002 [P0] 离线完成任务后同步
- **步骤**：
  1. 关闭网络
  2. 完成任务
  3. 打开网络
- **预期结果**：完成状态同步，目标进度更新
- **实测结果**：
- **备注**：

#### TC-SYNC-003 [P1] 弱网操作队列
- **步骤**：
  1. 使用弱网（限速 3G）
  2. 快速执行多个操作（创建任务、完成习惯、添加收件箱）
- **预期结果**：操作不丢失，按顺序重试，最终一致
- **实测结果**：
- **备注**：

#### TC-SYNC-004 [P1] 多端数据一致性
- **前置条件**：Android 和 Windows 同时登录同一账号
- **步骤**：
  1. 在 Android 创建任务
  2. 在 Windows 查看任务列表
- **预期结果**：Windows 端能拉取到新任务
- **实测结果**：
- **备注**：

#### TC-SYNC-005 [P2] 冲突处理
- **步骤**：
  1. 离线状态下两端同时修改同一任务标题
  2. 恢复网络
- **预期结果**：后提交者覆盖或产生可预期的冲突结果，不崩溃
- **实测结果**：
- **备注**：当前为简单 last-write-wins，需确认是否符合预期

---

### 4.15 Windows 桌面端模块

#### TC-WIN-001 [P0] Windows 启动
- **步骤**：
  1. 复制整个 `releases/windows/` 目录到测试电脑
  2. 双击 `planning_app_mobile.exe`
- **预期结果**：窗口正常打开，无崩溃，5 秒内显示登录/今日页
- **实测结果**：
- **备注**：

#### TC-WIN-002 [P0] Windows 登录
- **步骤**：
  1. 在 Windows 客户端登录测试账号
- **预期结果**：进入今日页，数据正常显示
- **实测结果**：
- **备注**：

#### TC-WIN-003 [P1] 页面切换
- **步骤**：
  1. 切换今日页、目标页、任务页、习惯页、AI 计划页、复盘页、更多页
- **预期结果**：各页面渲染正常，无白屏/布局错乱
- **实测结果**：
- **备注**：

#### TC-WIN-004 [P1] 窗口缩放
- **步骤**：
  1. 调整窗口大小、最大化、最小化
- **预期结果**：UI 自适应，内容不溢出
- **实测结果**：
- **备注**：

#### TC-WIN-005 [P1] Windows 离线操作
- **步骤**：
  1. 断开网络
  2. 创建任务/习惯
  3. 恢复网络
- **预期结果**：SQLite 本地数据库可用，操作入队，联网后同步
- **实测结果**：
- **备注**：

#### TC-WIN-006 [P2] Windows 缺少 DLL 检查
- **步骤**：
  1. 只复制 exe 文件到其他目录运行
- **预期结果**：提示缺少 DLL 或崩溃，验证必须复制整个目录
- **实测结果**：
- **备注**：破坏性测试，仅做一次性验证

---

### 4.16 后端 API/稳定性模块

#### TC-API-001 [P1] 健康检查
- **步骤**：
  1. 访问 `GET https://xutaostudy.xyz/api/v1/health`
- **预期结果**：返回 `{"status":"ok"}`
- **实测结果**：
- **备注**：

#### TC-API-002 [P1] Swagger 文档可访问
- **步骤**：
  1. 访问 `https://xutaostudy.xyz/docs`
- **预期结果**：Swagger UI 正常加载，接口列表完整
- **实测结果**：
- **备注**：

#### TC-API-003 [P2] 未授权访问
- **步骤**：
  1. 不带 token 调用 `GET /users/me`
- **预期结果**：返回 401 Unauthorized
- **实测结果**：
- **备注**：

#### TC-API-004 [P2] 监控指标
- **步骤**：
  1. 访问 `GET https://xutaostudy.xyz/api/v1/metrics`
- **预期结果**：返回 Prometheus 格式指标，不报错
- **实测结果**：
- **备注**：

---

## 5. 测试执行 Checklist

### Day 1：环境准备与基础流程

- [ ] 安装 Android APK 到真机和模拟器
- [ ] 复制 Windows 目录到测试电脑并启动
- [ ] 验证后端 `/health` 正常
- [ ] 执行冒烟测试：登录、今日页加载、目标/任务/习惯创建
- [ ] 记录环境信息（设备型号、Android 版本、Windows 版本、网络环境）
- [ ] 收集并保存首次启动日志

### Day 2：核心功能验证

- [ ] 完成登录/注册模块用例
- [ ] 完成今日页、目标、任务、习惯模块用例
- [ ] 完成 AI 计划、AI 复盘/洞察模块用例
- [ ] 完成收件箱、日历、提醒/通知模块用例
- [ ] 完成设置/偏好、报表模块用例
- [ ] 记录所有 P0/P1 失败用例

### Day 3：离线/同步/性能/回归

- [ ] 完成离线/同步模块用例
- [ ] 完成 Windows 桌面端模块用例
- [ ] 完成后端 API/稳定性模块用例
- [ ] 执行兼容性测试（不同分辨率、不同 Android 版本）
- [ ] 执行性能测试并记录启动时间、AI 响应时间、内存占用
- [ ] 修复后的回归测试
- [ ] 输出测试报告与缺陷清单

---

## 6. 缺陷记录模板

提交每个缺陷时建议包含以下字段：

```markdown
### BUG-XXX [优先级] 标题

- **模块**：登录/今日页/目标/...
- **平台**：Android / Windows
- **版本**：Week 27 APK / Windows exe
- **设备**：小米 14 / Win11 笔记本
- **网络**：Wi-Fi / 离线 / 弱网
- **复现步骤**：
  1. ...
  2. ...
- **期望结果**：
- **实际结果**：
- **日志片段**：
- **截图**：
- **状态**：新建/已确认/修复中/已验证
```

---

## 7. 真机测试已发现缺陷

以下缺陷来自 2026-08-15 Android 真机日志排查（详见 `docs/testing-phase.md` 第 19 节）。

### BUG-001 [P0] Android 端 Firebase/FCM 推送未真正初始化

- **模块**：提醒/通知模块
- **平台**：Android
- **版本**：Week 27 APK
- **设备**：vivo 真机（Android / ARM64）
- **网络**：Wi-Fi
- **复现步骤**：
  1. 安装 APK 并启动应用。
  2. 使用 `adb logcat --pid=$(adb shell pidof -s com.example.planning_app_mobile)` 抓取启动日志。
  3. 观察 `FirebaseInitProvider` 输出。
- **期望结果**：`FirebaseApp` 初始化成功，`FcmService` 可获取并上传 FCM token。
- **实际结果**：日志显示 `Default FirebaseApp failed to initialize because no default options were found`。
- **日志片段**：
  ```log
  W FirebaseApp: Default FirebaseApp failed to initialize because no default options were found.
    This usually means that com.google.gms:google-services was not applied to your gradle project.
  I FirebaseInitProvider: FirebaseApp initialization unsuccessful
  ```
- **根因**：Android 工程未应用 `com.google.gms.google-services` 插件，缺少 `google-services.json`。
- **状态**：已确认 / 待 Week 28 修复

### BUG-002 [P1] HealthPlugin 注册失败（ClassCastException）

- **模块**：运动数据模块
- **平台**：Android
- **版本**：Week 27 APK
- **设备**：vivo 真机（Android / ARM64）
- **网络**：Wi-Fi
- **复现步骤**：
  1. 启动应用。
  2. 查看 `GeneratedPluginRegistrant` 日志。
- **期望结果**：`health` 插件注册成功，`FitnessImportScreen` 可调用 Health Connect。
- **实际结果**：`GeneratedPluginRegistrant: Error registering plugin health, cachet.plugins.health.HealthPlugin`，抛出 `ClassCastException`。
- **日志片段**：
  ```log
  E GeneratedPluginRegistrant: Error registering plugin health, cachet.plugins.health.HealthPlugin
  E GeneratedPluginRegistrant: java.lang.ClassCastException
    at a4.c.d(...)
    at k2.l.onAttachedToActivity(...)
  ```
- **根因**：`health` 插件与当前 Flutter 嵌入版本 / Activity 类型 / compileSdk 不兼容。
- **状态**：已确认 / 待 Week 28 修复

### BUG-003 [P2] Invalid resource ID 0x00000001

- **模块**：UI 资源加载
- **平台**：Android
- **版本**：Week 27 APK
- **设备**：vivo 真机（Android / ARM64）
- **网络**：Wi-Fi
- **复现步骤**：
  1. 启动应用并进行页面切换/点击操作。
  2. 观察日志中的 `ning_app_mobile` 标签。
- **期望结果**：无资源加载错误。
- **实际结果**：约 6 分钟内出现 4 次 `Invalid resource ID 0x00000001`。
- **日志片段**：
  ```log
  E ning_app_mobile: Invalid resource ID 0x00000001.
  ```
- **根因**：代码引用了未声明的 asset/drawable，或第三方库使用了无效默认资源句柄。
- **状态**：已确认 / 待 Week 28 排查

### BUG-004 [P2] 未启用 Android 13+ 预测性返回手势

- **模块**：系统导航
- **平台**：Android
- **版本**：Week 27 APK
- **设备**：vivo 真机（Android / ARM64）
- **网络**：Wi-Fi
- **复现步骤**：
  1. 启动应用。
  2. 查看 `WindowOnBackDispatcher` 日志。
- **期望结果**：无兼容性警告。
- **实际结果**：出现 23 次 `OnBackInvokedCallback is not enabled for the application`。
- **日志片段**：
  ```log
  W WindowOnBackDispatcher: OnBackInvokedCallback is not enabled for the application.
  W WindowOnBackDispatcher: Set 'android:enableOnBackInvokedCallback="true"' in the application manifest.
  ```
- **修复方向**：在 `AndroidManifest.xml` 的 `<application>` 标签添加 `android:enableOnBackInvokedCallback="true"`。
- **状态**：已确认 / 待 Week 28 修复

---

## 8. 通过/失败标准

### 通过标准

- 所有 P0 用例 100% 通过
- P1 用例通过率达到 90% 以上
- 无阻塞性缺陷（导致无法完成核心流程）
- Android 崩溃率 < 1%（10 次启动中无崩溃）
- Windows exe 可稳定启动并运行 30 分钟
- 服务端 `/health` 7×24 可用

### 失败标准（需修复后才能进入下一阶段）

- 任一 P0 用例失败
- 登录/注册/今日页无法使用
- 离线操作导致数据丢失
- 服务端核心 API 持续不可用
- 构建产物无法安装或启动即崩溃

---

## 9. 风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| 服务器性能不足导致 API 超时 | 中 | 高 | 测试时记录响应时间，超过 5 秒截图；必要时重启服务或错峰测试 |
| VPN/网络不稳定影响 GitHub/DeepSeek 调用 | 中 | 中 | 优先使用本地模板和离线功能，AI 测试在稳定网络下执行 |
| DeepSeek API 额度不足 | 低 | 高 | 关注 `AI_DAILY_COST_LIMIT_USD`，触发上限后验证模板降级 |
| 测试数据污染导致结果不准 | 中 | 中 | 使用专用测试账号，必要时重置数据；记录测试数据范围 |
| 不同 Android 厂商通知权限行为差异 | 高 | 中 | 在多个品牌真机上验证通知触发 |
| Windows 缺少运行库导致启动失败 | 中 | 中 | 确保复制整个 `windows/` 目录，必要时安装 VC++ Redistributable |
| Firebase/FCM 未配置导致远程推送不可用 | 已确认 | 高 | Week 28 应用 `google-services` 插件并补充 `google-services.json` |
| `health` 插件 ClassCastException 导致运动同步失效 | 已确认 | 中 | Week 28 排查插件版本/Activity 类型兼容性 |
| 资源 ID 无效导致偶发资源加载错误 | 已确认 | 低 | Week 28 排查 `pubspec.yaml` assets 与第三方库默认资源 |

---

## 10. 测试产物

测试完成后输出：

1. **测试报告**（可基于本文档填写实测结果）
2. **缺陷清单**（使用 BUG 模板）
3. **性能数据表**：启动时间、AI 响应时间、内存占用、CPU 占用
4. **设备/环境清单**：测试机型、Android 版本、Windows 版本、网络环境
5. **关键日志文件**：Android `adb logcat` 输出、Windows `app.log`

---

## 11. 附录

### 11.1 常用命令

```bash
# 安装 APK
adb install -r releases/planning-app-week27.apk

# 启动应用
adb shell am start -n com.example.planning_app_mobile/.MainActivity

# 查看 Flutter 日志
adb logcat -s flutter

# 按应用包名过滤日志
adb logcat --pid=$(adb shell pidof -s com.example.planning_app_mobile)

# 保存日志到文件
adb logcat -v threadtime > app.log

# 后端健康检查
curl -s https://xutaostudy.xyz/api/v1/health

# Windows 运行并查看日志
cd releases\windows
.\planning_app_mobile.exe > app.log 2>&1
```

### 11.2 测试账号

| 字段 | 值 |
|------|-----|
| 邮箱 | `planning-test@example.com` |
| 密码 | `Test@123456` |

### 11.3 参考文档

- [`testing-phase.md`](./testing-phase.md)：测试阶段构建、测试方法、观察指标、Week 27 构建修复记录、Android 真机日志排查
- [`handover-summary.md`](./handover-summary.md)：项目整体交接、环境、进度与部署记录
- [`项目阶段总结.md`](../../项目阶段总结.md)：开发阶段状态与未实现功能
- [`development-roadmap.md`](./development-roadmap.md)：后续 Week 修复与开发计划
